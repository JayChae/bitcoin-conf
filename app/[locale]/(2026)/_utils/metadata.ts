import { createPageMetadata } from "@/app/_utils/seo";
import { seoMessages } from "@/app/messages/seo";
import { event } from "@/app/messages/2026/event";

export const pageMetadata = createPageMetadata({ seoMessages, event });
